
credentials:
  my-lambda-creds:
    provider: "lambdalabs"
    api_key: "your-lambda-api-key-here"
    default_location: "us-west-1"
    
  prod-lambda:
    provider: "lambdalabs"
    api_key: "your-prod-api-key"
    default_location: "us-east-1"
    ref_id: "production-lambda"  # Optional explicit override

settings:
  output_format: "yaml"
  default_timeout: "5m"
